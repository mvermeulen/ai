[project]
name = "wordpress-local-plan-sync"
version = "0.2.0"
description = "Alternative offline-first WordPress workflow using immutable sync plans"
requires-python = ">=3.11"

[project.scripts]
wp-local-plan = "wp_local_v2.cli:main"

[tool.pytest.ini_options]
pythonpath = ["."]
testpaths = ["tests"]
